PerfectOutfit — Progress Log

2026-09-19 (4)
- Implemented issue #13 (Drive backup service: decisions, DriveClient seam, fake, tests).
  Changes:
    * New `feature/backup/`: `DriveClient` (+ `DriveFile`), `BackupStateStore` (last time +
      hash, `clear()`), `BackupFilename` ("yyMMdd-HHmm PerfectOutfit.json", java.time, strict
      parse), `BackupRetention` (newest 9 = 7 daily + 1 weekly + 1 monthly), and a plain
      `DriveBackupService` (backup/listSnapshots/restore/onDisconnect; no Hilt binding yet
      since there is no real DriveClient until #16). backup() skips only when the hash
      matches AND the folder lists >= 1 parsable backup; unrelated files are ignored/never pruned.
    * Every DAO getAll() used by exportToJson now has a total ORDER BY (added id / composite
      key tie-breakers; outfit_items had none) so identical data serializes identically.
    * Tests (plain JVM): BackupFilenameTest, BackupRetentionTest, DriveBackupServiceTest (10
      cases) with FakeDriveClient and FakeBackupStateStore under app/src/test.
  Not done: "newer snapshot rejected" lives in #14; real client/auth/worker/UI in #16.
  Verification: ./gradlew testDebugUnitTest passes.

2026-09-19 (3)
- Implemented issue #11 (Forecast module: one owner for known hours, display window,
  per-date fetch, location).
  Changes:
    * New `feature/forecast/Forecast` (@Singleton, OpenMeteoApi + injected java.time.Clock,
      provided in AppModule): `location` (nullable ForecastLocation; no 0.0 sentinel),
      `locationFlow`, `hours` (reactive, sorted), `refresh(lat, lon, name)`, `displayWindow()`,
      `hoursFor(date)` (fetch + merge only when the date is missing, stale-location safe),
      `today()`, and companion `hourNearest` (exact hour, else last before, else first).
    * WeatherRepository and WeatherMapper.extractDisplayedHours deleted. HomeViewModel uses
      refresh/displayWindow, takes its location name from forecast.locationFlow, and the
      duplicated getCurrentLocation fallback is one `requestFreshLocation`.
    * RateOutfitViewModel holds `hoursForSelectedDate` + a per-day selectedHourIndex (the
      global-index indirection and selectHour are gone); selectDate/init use hoursFor; live
      wizard now starts on the date of the handed-off hour. logLat/logLon dropped from state.
    * LiveOutfitPayload shrunk to selectedHourTime, workoutDurationHours, prefillItemIds.
    * Tests: FakeOpenMeteoApi (testutil) + ForecastTest (14 cases, plain JVM). CONTEXT.md:
      "Forecast" and "Display window" entries, wizard-up-to-today rule.
  Not done: typed error replacing the "permission" substring match (left as the issue allows).
  Verification: ./gradlew testDebugUnitTest passes. Manual device check not done (no emulator).

2026-09-19 (2)
- Implemented issue #10 (Outfit logging module: one path for snapshot, entry, reminder).
  Changes:
    * New `feature/outfit/OutfitLogging` (@Singleton; DAOs + DatabaseTransactionRunner +
      RatingReminder): log(hour, LogLocation, sport, itemIds, rating, notes, duration,
      LogMode.LIVE/PAST), rate, update (items+notes+rating in one transaction), delete,
      restore, plus read pass-throughs. `createdAt` = workout hour for every log (Home used
      the wall clock before; documented in CONTEXT.md "Outfit entry"). Reminder only for
      LIVE or unrated logs, now enforced for Home too. Rating cancels the reminder.
    * OutfitRepository deleted; WeatherRepository lost saveSnapshot and its DAO dependency.
      HomeViewModel.acceptRecommendation / RateOutfitViewModel.save are one call each; dead
      saveWeatherForLater and buildSnapshot gone. "Current Location" fallback lives only in
      LogLocation. HistoryViewModel and RatingActionHandler go through the module.
    * RateOutfitViewModel no longer uses first() on the apparent preference: likelyItemIds
      is derived reactively from (selected hour, sport, preference).
    * Tests: OutfitLoggingTest (13 cases, plain JVM, DAO fakes + FakeRatingReminder);
      existing handler/receiver tests rewired; FakeOutfitEntryDao.updateNotes implemented.
  Verification: ./gradlew testDebugUnitTest passes. Manual device check not done (no
  emulator in this session): accept recommendation, wizard live/past, rate from History,
  undo delete.

2026-09-19 (1)
- Implemented issue #9 (Recommendation module: one entry point and one reference-temperature
  rule for Home and Explorer).
  Changes:
    * New `feature/recommendation/Recommendations` (@Singleton, depends only on
      OutfitEntryDao): find(sport, rawTemp, useApparent), findForWorkoutWindow(...) (coldest/
      warmest indices + recommendations, returns null if empty or if an `isCurrent` callback
      turns false mid-query = the staleness rule), likelyItemIds, stops, and a companion
      `workoutExtremes` (formerly private HomeViewModel.computeExtremes; later hour wins ties).
      RecommendationPolicy moved into the package and made `internal`.
    * New core/model/WeatherReading interface (the seven measurements) implemented by both
      HourlyWeather and WeatherSnapshot, plus one `referenceTemp(useApparent)` extension.
      OutfitEntryWithDetails.roundedTemp deleted; rounding happens only inside the module.
    * OutfitRepository lost findRecommendation/getRatedEntries/getLikelyItemIds. HomeViewModel
      drops the `adjustedApparentTemp` Int shadow and uses one findForWorkoutWindow call
      (duration 1 -> coldest). ExplorerViewModel now collects the preference reactively (was
      first()) and uses stops()/find(). RateOutfitViewModel calls recommendations.likelyItemIds
      (its own first() read is left for #10).
    * Tests: RecommendationPolicyTest -> RecommendationsTest (same 20 cases through the module);
      new RecommendationsWindowTest (extremes tie-breaks, window, staleness, stops, rounding).
      FakeOutfitEntryDao.getRatedEntriesWithDetails now implemented (+ onQuery hook).
    * CONTEXT.md: reference-temp rule location, reactivity claim, stale DAO note removed,
      new "Recommendation" entry.
  Verification: ./gradlew testDebugUnitTest passes. Manual check of Home/Explorer on a device
  not done (no emulator in this session).

2026-09-18 (3)
- Implemented issue #8 ("Export/import: single schema definition, safe import,
  round-trip test"). ExportImportManager previously defined four hand-written DTOs that
  mirrored the four Room entities field-by-field, spelling each field out three times
  (entity, export mapping, import mapping); import also deleted all four tables before
  parsing had fully validated the file.
  Changes:
    * ClothingItem, WeatherSnapshot, OutfitEntry, OutfitItem, Sport, BodyPart: added
      `@Serializable` directly to the Room entities/enums (core/model). They're now
      serialized as-is instead of through mirror DTOs — each field exists in exactly one
      place. Enum JSON output is unchanged (kotlinx serializes an enum by its `.name`,
      same string `Sport.valueOf`/`BodyPart.valueOf` used to read/write), so the JSON
      shape is byte-for-byte compatible with files the old code produced.
    * ExportImportManager.kt: `ExportData` gains a `version` field
      (`CURRENT_EXPORT_VERSION`, currently 1) marked `@EncodeDefault(...ALWAYS)` so it's
      always written even though it also has a default (needed for old files, which lack
      the key, to still decode). Manager now depends on the four DAOs and a
      `DatabaseTransactionRunner` directly instead of CatalogRepository/OutfitRepository/
      WeatherRepository (acceptable per the issue — those repos are slated for dissolution
      by #9/#10). `importFromJson` decodes+validates the whole file first (a parse or
      unknown-enum-constant failure now throws before any delete runs), then does the
      delete-then-insert inside one `transactionRunner.runInTransaction { }` block.
    * DatabaseTransactionRunner.kt (new interface) / RoomTransactionRunner.kt (new,
      `AppDatabase.withTransaction`) / core/di/DatabaseModule.kt (new `@Binds`): same
      interface-plus-Android-adapter seam issue #6 used for RatingReminder, so the
      round-trip test can run on the plain JVM instead of needing Robolectric for a real
      Room transaction.
    * CatalogRepository/OutfitRepository/WeatherRepository: removed the getAll/insertAll/
      deleteAll pass-through methods that existed only for ExportImportManager and are now
      unused.
    * Moved FakeOutfitEntryDao/FakeOutfitItemDao from core/notification (app/src/test) to
      a new shared app/src/test/.../testutil package per the issue's suggestion, since
      export/import needed them too; gave FakeOutfitItemDao working implementations
      instead of UnsupportedOperationException (nothing tested the throwing versions).
      Added FakeClothingItemDao, FakeWeatherSnapshotDao, FakeDatabaseTransactionRunner
      (runs the block inline, no real transaction) alongside them.
    * ExportImportManagerTest.kt (new): round-trip test (export through fakes, import into
      fresh fakes, assert all four tables equal), a test that a malformed JSON string
      leaves the seeded fakes untouched, a test that a hand-written fixture in the old
      (pre-#8, no "version" key) shape still imports correctly, and a test that exported
      JSON contains the version key.
    * CONTEXT.md: added an "Export/import schema" glossary entry.
  Verified: ./gradlew testDebugUnitTest and ./gradlew assembleDebug both succeed (Hilt
  graph resolves with the new DatabaseModule @Binds, Room accepts the @Serializable
  entities). Did not manually verify export/import on-device (no emulator in this
  environment) — worth a quick manual round-trip check, and specifically confirming an
  old on-disk backup file (from before this change) still imports.

